[workspace]
resolver = "2"
members = [
    "nixpanel-common",
    "nixpanel-api",
    "nixpanel-accounts",
    "nixpanel-services",
    "nixpanel-sysinfo",
]

[profile.release]
opt-level     = 3
lto           = true
codegen-units = 1
strip         = true
panic         = "abort"
